apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: mcp-test-service-2
  namespace: default
  description: Second test service registered end-to-end via Backstage MCP tools
  tags:
    - test
    - mcp
    - service
  annotations:
    backstage.io/source-location: "url:https://github.com/talkitdoit/ai-meets-backstage-catalog-to-mcp"
spec:
  type: service
  lifecycle: experimental
  owner: group:default/guests
  system: examples
